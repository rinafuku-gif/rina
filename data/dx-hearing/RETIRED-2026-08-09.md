# rina旧DX経路 退役記録

- 退役日: 2026-08-09
- 実施: しらたま（Ryo GO済み）
- 理由: SATOYAMA DXパイプライン（satoyama-web側）がフェーズ1〜4まで完成・本番稼働。DX入口を新窓口に一本化するため、rina側の旧DX経路を閉じた。
  - rina旧経路の実装中核（`runDxImplementation` 等）は機械移行で既に壊死（CLAUDE_PATH・DX_PROJECTS_DIRが旧MBPパスのまま）。生きていたのは入口受付・簡易提案生成・Discord通知のみ。
  - 実データはテスト2件（`DX-20260315-FORM_T-*`, `DX-20260315-hy9w1t-*`）・2026-03-20頃で停止。直近30日はおろか、ログ全量（4,515,110行・2026-08-09時点）を通じて `/api/dx-hearing` への実アクセス0件、`[DX Cases] Status updated` ログ0件を確認済み。
- 新しい窓口: https://web.satoyama-ai-base.com/dx-intake/

## 閉じたエンドポイント（`scripts/line-webhook-server.js`）

- `POST /api/dx-hearing`（DXヒアリング受付） → 410 Gone
- `POST /api/dx-cases/:caseId/status`（しらたまDXタブからのGO実行・修正実行トリガー） → 410 Gone

読み取り系（`GET /api/dx-cases`, `GET /api/dx-cases/:id`, `GET /proposal/:caseId`, `GET /preview/:caseId`, コメント関連）・削除系（`DELETE /api/dx-cases/:caseId`）は未変更（ダッシュボード表示を壊さないため）。

## ロールバック手順

このコミット（メッセージ: 「rina旧DX経路を退役（410 Gone・新窓口はsatoyama-web /dx-intake/）」）を `git revert` する。
または `/Users/ocmm/rina/scripts/line-webhook-server.js.bak-20260809` を `scripts/line-webhook-server.js` へ上書きコピーし、`launchctl kickstart -k gui/$(id -u)/com.rina.line-webhook-server` で再起動する。
